server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/bookstore
    username: bookuser
    password: bookpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
